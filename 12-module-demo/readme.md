# Module demo

## Arguments
Instances -- Not mandatory optional, User can also override, default is as below

mysql = "t3.small"

backend = "t3.micro"

frontend = "t3.micro"

ami -- Mandatory user must supply the ami_id

## Outputs
Public_IP -- Public IP of instance created

Private IP -- Private IP of instance created

Instance ID -- Instance_ID of instance created

